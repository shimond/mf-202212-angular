import { GradeToColorPipe } from './grade-to-color.pipe';

describe('GradeToColorPipe', () => {
  it('create an instance', () => {
    const pipe = new GradeToColorPipe();
    expect(pipe).toBeTruthy();
  });
});
